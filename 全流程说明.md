# 智能物流机器人 全流程说明文档

---

## 一、系统架构

```
┌──────────────┐       UART1 9600bps        ┌──────────────┐
│   STM32F407  │◄──────────────────────────►│   RDK X5     │
│   (底层控制)  │   0xFF + 9字节ASCII + 0xFE  │   (视觉识别)  │
│              │   单字节命令                  │              │
│  ● CAN总线   │                              │  ● OpenCV    │
│  ● PID控制   │                              │  ● pyzbar    │
│  ● 麦轮运动  │                              │  ● 串口通信   │
│  ● 舵机控制  │                              │              │
└──────────────┘                              └──────────────┘
```

**数据帧格式：**  
- STM32 → RDK X5：1字节 ASCII 命令  
- RDK X5 → STM32：`0xFF` + 9字节 ASCII 数据 + `0xFE`

视觉主程序：[`视觉程序/gongchuang_vision.py`](./视觉程序/gongchuang_vision.py)  
下摄像头 `/dev/video0` 扫码，上摄像头 `/dev/video2` 做物块 / 色环 / 码垛定标，串口 `/dev/ttyUSB0`。

---

## 二、当前版本说明

| 文件 | 说明 |
|------|------|
| `电控程序/BMH-初赛` / `电控程序/BMH-决赛` | STM32 工程。无视觉版任务码可硬编码，跳过视觉通信，只跑一轮 |
| [`视觉程序/gongchuang_vision.py`](./视觉程序/gongchuang_vision.py) | RDK X5 视觉主程序，含完整通信协议和任务分发 |

> 被注释掉的 `wait_send_shumeipai()` 等代码，在下方表格中用 **[注释]** 标记，表示完整视觉版才会执行。

---

## 三、完整通信协议表

| STM32 发送 | 触发时机 | RDK X5 收到后执行 | RDK X5 发回数据 | STM32 收到后操作 |
|:---:|------|------|------|------|
| `0x31` | **[注释]** 扫码阶段 | `run_erweima()` 打开 `/dev/video0`，pyzbar 识别二维码，连续 3 帧相同确认 | `"123+3210"` → 7 位任务码再补齐 | 解析 task[0..5] |
| `0x32` | **[注释]** 圆台物块定标 | `run_wukuaiyuanxin_1()` HoughCircle + HSV，位置稳定 30 帧 + 颜色置信度≥70% | `"095505191"` → XXXX YYYY C R | 解析像素坐标+颜色，`C_1=颜色` |
| `0x33` | **[注释]** 转盘排序(第一圈) | `LOOP1()` → 等转盘转 → `run_wukuaiyuanxin_A()` 快扫 3 位置颜色 → 判断旋转方向 → 匹配任务码前三位 | `"312456789"` → INDEX INDEX1 INDEX2 | 解析为 ypcode[0..2] |
| `0x34` | **[注释]** 转盘排序(第二圈) | `LOOP2()`，逻辑同第一圈，对照任务码后三位 | `"312456789"` | 解析为第二圈 ypcode |
| `0x35` | **[注释]** 第一圈确认第2物块 | `run_wukuaiyuanxin_xuanzequyu()` 验证指定区域颜色是否 = 任务码 | `"987654321"` | 收到"98" → 确认通过，执行抓取 |
| `0x36` | **[注释]** 第一圈确认第3物块 | 同上，验证第3个位置的颜色 | `"987654321"` | 同上 |
| `0x37` | **[注释]** 色环粗定标 | `run_sehuanyuanxin2()` 640×480, r=35~38, 位置+颜色稳定性确认 | `"095505191"` → XXXX YYYY C | 像素坐标 ×1.18 → 实际距离 |
| `0x64` | **[注释]** 角度校正 | 当前视觉端未跑 `detect_boundary_line()`，直接回全零 | `"000000000"` | 现场按 0° 处理 |
| `0x38` | **[注释]** 色环精定标 | `run_sehuanyuanxin2_centered()` 1920×1080，半分辨率粗检 + 全分辨率局部精修 | `"095505191"` | 像素坐标 ×0.29 → 细调距离 |
| `0x39` | **[注释]** 码垛定标 | `run_maduoyuanxin2()` 640×480, r=20~27 | `"095505191"` | 码垛位置定位 |
| `0x30` | **[注释]** 码垛精定标 | 同上 | `"095505191"` | 码垛精确定位 |
| `0x61` | **[注释]** 第二圈物块定标 | `run_wukuaiyuanxin_1()` 结果存 C_1_loop2 | 同上 0x32 | 第二圈坐标 |
| `0x62` | **[注释]** 第二圈确认第2物块 | 同 0x35 逻辑 | `"987654321"` | 确认 |
| `0x63` | **[注释]** 第二圈确认第3物块 | 同 0x36 逻辑 | `"987654321"` | 确认 |
| `0x72` | 重启视觉 | `restart_program()` 关串口，20 秒后重启 | — | — |
| (初始化) | 程序启动时 | `send_data("987654321")` 握手信号 | — | STM32 可据此判断视觉端就绪 |

颜色约定：`1` 红、`2` 绿、`3` 蓝。识别失败超时回 `"000000004"`。

---

## 四、完整任务流程

> 📌 当前部分电控工程仍是**无视觉简化版**，任务码硬编码为 `红=1, 绿=2, 蓝=3`，视觉通信大部分被注释。下方列出完整版流程（注释代码 + 当前执行代码结合）。视觉端本身已按完整协议实现。

---

### 阶段0：系统初始化

| 步骤 | 操作 | 说明 |
|:---:|------|------|
| 0.1 | `delay_init(168)` | 168MHz系统时钟 |
| 0.2 | `board_init()` | CAN总线 500Kbps 初始化 |
| 0.3 | `LED_Init()` | LED指示灯 |
| 0.4 | `OLED_Init()` | OLED屏幕 |
| 0.5 | `Timer6_Init()` | delay_ms1 定时器 |
| 0.6 | `PWM_Init()` | 舵机PWM (TIM3, 20ms周期) |
| 0.7 | `uart5_init(9600)` | 调试串口 |
| 0.8 | `uart6_init(9600)` | HMI串口屏 |
| 0.9 | `Serial_Init()` | UART1 (RDK X5 通信) |
| 0.10 | `Swith_Init()` | 限位开关 |
| 0.11 | `imu_init()` | WHT101 IMU (USART2) |
| 0.12 | `PIDInit()` | PID定时器 (TIM4, 50ms周期) |
| 0.13 | `Key_EXTI_Init()` | 外部按键中断 |
| 0.14 | `dianji_move_flag = 1` | 使能电机 |

---

### 阶段0.5：等待就绪

| 步骤 | 动作 | STM32 发送 | RDK X5 发回 | 成功 | 失败处理 |
|:---:|------|:---:|------|------|------|
| — | OLED显示 `"OK-noeye"`，LED闪烁等待 | — | — | 用户按下限位开关 | 一直等待 |
| — | `WHT101_ANGLEZCali()` | — | — | IMU零位校准完成 | — |
| — | `TIM_SetCompare1(TIM3, 300)` | — | — | 补光灯打开 | — |

---

### 阶段1：二维码扫描 & 任务获取

#### 完整版流程（被注释）
| 步骤 | 动作 | STM32 发送 | RDK X5 执行 | RDK X5 发回 | 成功 | 失败 |
|:---:|------|:---:|------|------|------|------|
| 1.1 | `car_move2(150,0,150,150)` 右移150mm | — | — | — | — | — |
| 1.2 | `car_move1(0,-530,150,200)` 后退530mm | — | — | — | — | — |
| 1.3 | 发送扫码命令 | `0x31` | `run_erweima()` | `"123+3210"` | 解析 task[0..5] 并串口屏显示 | RDK X5 发 `"二维码数据错误"`，无处理 |
| 1.4 | 存储平台归零 | — | — | — | `set_wukuaipingtai_Angle(20, 300)` | — |

#### 当前简化版（实际执行）
```
task[0]=1  task[1]=2  task[2]=3       ← 硬编码，无通信
task[3]=1  task[4]=2  task[5]=3       ← 硬编码，无通信
```

---

### 阶段2：圆台取料（第一轮3个物块）

| 步骤 | 动作 | STM32 发送 | RDK X5 执行 | RDK X5 发回 | 成功 | 失败处理 |
|:---:|------|:---:|------|------|------|------|
| 2.1 | `set_zhuashou_Angle(25, 300)` | — | — | — | 爪子张开 | — |
| 2.2 | `car_move1(0, -910, 150, 200)` → 后退910mm | — | — | — | 退到观察位 | — |
| 2.3 | `move_all(85, ..., 250, 10)` → 升臂，云台转250° | — | — | — | 对准转盘 | — |
| 2.4 | **[注释]** 发物块定标 | `0x32` | `run_wukuaiyuanxin_1()` | `"XXXXYYYYCR"` | 粗定位完成 | `Serial_RxPacket[8]==0x34`→重发 |
| 2.5 | `car_move2(-30, 0, 100, 100)` → 左移30mm微调 | — | — | — | — | — |
| 2.6 | **[注释]** 发转盘排序 | `0x33` | `LOOP1()` | `"312456789"` | ypcode=[3,1,2] | 超时等20s |
| 2.7 | **当前简化版**: ypcode[0]=1, ypcode[1]=2, ypcode[2]=3 | — | — | — | 硬编码 | — |
| 2.8 | `delay_ms1(1000)` 等待1秒 | — | — | — | — | — |

---

#### 2A：抓取第1个物块（ypcode[0]=1 → 红色区）

| 步骤 | 动作 | 说明 |
|:---:|------|------|
| 2A.1 | `yuantai_na(1)` → 爪子张开25° → 伸臂-20mm → 爪子闭合1.5° → 收臂回72° → 爪子张开25° | 完整抓取动作 |
| 2A.2 | `move_all(0, ..., 72, 13)` → 收臂归位 | 回到安全位 |
| 2A.3 | `set_wukuaipingtai_Angle(144, 300)` → 物块存储平台转144° | 第1个存储工位 |

#### 2B：抓取第2个物块（ypcode[1]=2 → 绿色区）

| 步骤 | 动作 | STM32 发送 | RDK X5 执行 | RDK X5 发回 | 成功 | 失败 |
|:---:|------|:---:|------|------|------|------|
| 2B.1 | `yuantai_na1(2)` → 爪子张开25° → 伸臂对准绿位(235°) | — | — | — | 对准完成 | — |
| 2B.2 | **[注释]** 颜色确认 | `0x35` | `run_wukuaiyuanxin_xuanzequyu()` | `"987654321"` | 确认颜色匹配 | 重发等待 |
| 2B.3 | `yuantai_na2(2)` → 抓取闭合(1.5°) → 收回 | — | — | — | 绿色物块抓取完成 | — |
| 2B.4 | `set_wukuaipingtai_Angle(258, 300)` → 平台转258° | — | — | — | 第2个存储工位 | — |

#### 2C：抓取第3个物块（ypcode[2]=3 → 蓝色区）

| 步骤 | 动作 | STM32 发送 | RDK X5 执行 | RDK X5 发回 |
|:---:|------|:---:|------|------|
| 2C.1 | `yuantai_na1(3)` → 对准蓝位(276°) | — | — | — |
| 2C.2 | **[注释]** 颜色确认 | `0x36` | 验证第3位置 | `"987654321"` |
| 2C.3 | `yuantai_na2(3)` → 抓取 | — | — | — |

#### 取料完成

| 步骤 | 动作 |
|:---:|------|
| 2D.1 | `TIM_SetCompare1(TIM3, 0)` → 关补光灯 |
| 2D.2 | `move_all1(0,...,72,13)` → 臂归位 |
| 2D.3 | `set_zhuashou_Angle(30, 200)` → 爪子半开 |
| 2D.4 | `set_wukuaipingtai_Angle(20, 300)` → 平台归零 |

---

### 阶段3：穿越1700mm通道

| 步骤 | 动作 | PID参数 | 说明 |
|:---:|------|:---:|------|
| 3.1 | `PID_move(-30, 0, 0, 400, 0, 1)` | kp=3.3, kd=1.8, max=30 | 后退靠墙 |
| 3.2 | `delay_ms1(200)` | — | 等待200ms |
| 3.3 | `car_move1(0, 400, 150, 200)` | — | 前进400mm |
| 3.4 | `PID_move(0, 0, 0, 1000, 90, 0)` | kp=2.0, kd=0.8 | **原地旋转90°** (对准通道) |
| 3.5 | `PID_move(0, 0, 0, 1000, 90, 3)` | kp=2.0, ki=0.05, kd=0.4 | 微调确认90° |
| 3.6 | `car_move1(0, -1700, 150, 250)` | — | **穿越1700mm通道** |
| 3.7 | `PID_move(0, 0, 0, 1000, -180, 0)` | kp=2.0, kd=0.8 | **原地旋转180°** (面向色环) |
| 3.8 | `PID_move(0, 0, 0, 1000, -180, 3)` | kp=2.0, ki=0.05, kd=0.4 | 微调确认180° |

---

### 阶段4：色环放置（第一轮3个物块）

#### 视觉定位（完整版，当前被注释）

| 步骤 | STM32 发送 | RDK X5 执行 | 发回 | STM32 操作 | 失败处理 |
|:---:|:---:|------|------|------|------|
| 粗定标 | `0x37` | `run_sehuanyuanxin2()` | `"XXXXYYYYC"` | `direct = 像素差×1.18` | 未识别→重发 |
| 角度校正 | `0x64` | 当前直接回 `"000000000"` | `"000000000"` | 现场按 0° 处理 | 未启用直线检测 |
| 精定标 | `0x38` | `run_sehuanyuanxin2_centered()` | `"XXXXYYYYC"` | `direct = 像素差×0.29` | 未识别→不移动 |

#### 当前简化版（无视觉）→ 直接角度校准后放置

| 步骤 | 动作 | 说明 |
|:---:|------|------|
| 4.1 | `PID_move(0, 0, 0, 1000, -180, 4)` | kp=2.0, ki=0.07, kd=0.4 角度校准 |
| 4.2 | `set_zhuashou_Angle(25.5, 200)` | 爪子半开 |
| 4.3 | `move_all(0,...,35,...,72,13)` | 臂伸出对准色环 |

#### 4A：放置第1个物块（task[0]=1 → 红色环）

| 步骤 | 动作 |
|:---:|------|
| 4A.1 | `set_zhuashou_Angle(1.5, 200)` → 爪子闭合抓住 |
| 4A.2 | `fangzhi(1)` → `fangzhi_red()` → 臂伸向193°（红色色环位置） |
| 4A.3 | `set_zhuashou_Angle(25, 300)` → 松开物块 |
| 4A.4 | `set_wukuaipingtai_Angle(143, 300)` → 存储平台转143° |
| 4A.5 | `shengjiang_control(0)` → 升降复位 |
| 4A.6 | `move_all(...)` → 臂收回72° |

#### 4B：放置第2个物块（task[1]=2 → 绿色环）

| 步骤 | 动作 |
|:---:|------|
| 4B.1 | `fangzhi(2)` → `fangzhi_green()` → 臂伸向247.9° |
| 4B.2 | 平台转258°，臂收回 |

#### 4C：放置第3个物块（task[2]=3 → 蓝色环）

| 步骤 | 动作 |
|:---:|------|
| 4C.1 | `fangzhi(3)` → `fangzhi_blue()` → 臂伸向300.5° |
| 4C.2 | 平台归零20°，臂归位193° |

---

### 阶段5：色环取回

| 步骤 | 动作 | 说明 |
|:---:|------|------|
| 5.1 | `na(task[0])` → `na_red()` → 伸臂到193° → 闭合1.5°抓取 → 收回72° | 取回红色位 |
| 5.2 | 平台转143° | |
| 5.3 | `na(task[1])` → `na_green()` → 伸臂247.9° → 抓取 → 收回 | 取回绿色位 |
| 5.4 | 平台转258° | |
| 5.5 | `na(task[2])` → `na_blue()` → 伸臂300.5° → 抓取 → 收回 | 取回蓝色位 |
| 5.6 | 平台归零，爪子张开25° | 复位 |

---

### 阶段6：返回圆台区

| 步骤 | 动作 | PID | 说明 |
|:---:|------|:---:|------|
| 6.1 | `PID_move(-30, 0, 0, 300, -180, 1)` | kp=3.3 | 后退 |
| 6.2 | `PID_move(0, 0, 0, 500, -180, 3)` | kp=2.0, ki=0.05 | 微调 |
| 6.3 | `car_move1(140, 820, 180, 180)` | — | 走蓝线 |
| 6.4 | `PID_move(0, 0, 0, 1000, 90, 0)` | kp=2.0 | **旋转90°** |
| 6.5 | `PID_move(0, 0, 0, 1000, 90, 3)` | 带积分 | 微调 |
| 6.6 | `car_move1(140, 760, 180, 150)` | — | 前进 |

---

### 阶段7：第二轮色环放置

> 与阶段4 完全一致（重新放置 task[0], task[1], task[2]），最终复位。

---

### 阶段8：返回圆台

| 步骤 | 动作 | 说明 |
|:---:|------|------|
| 8.1 | `PID_move(-30, 0, 0, 300, 90, 1)` | 后退 |
| 8.2 | `car_move1(140, 870, 180, 180)` | 走蓝线 |
| 8.3 | `PID_move(0, 0, 0, 1000, 0, 0)` | **旋转0°** |
| 8.4 | `PID_move(0, 0, 0, 1000, 0, 3)` | 微调 |
| 8.5 | `car_move1(140, 390, 180, 180)` | 前进 |
| 8.6 | `move_all1(40, ..., 278, 13)` | 云台转278° |
| 8.7 | `PID_move(30, 0, 0, 300, 0, 1)` | 前进靠墙 |

---

### 阶段9：jixie_reset() → 程序结束

```
while(swith_out()) { LED闪烁 }  → 等待限位开关触发
set_zhuashou_Angle(15, 300)     → 爪子半闭
move_all(0,...,72,13)           → 臂归位
set_wukuaipingtai_Angle(20,300) → 平台归零
while(1){}                       → 死循环结束
```

> ⚠️ 当前代码 `jixie_reset()` 末尾有 `while(1){}`，执行后程序在此终止，**第二轮不会运行**。

---

## 五、通信成功/失败处理汇总

| 场景 | STM32 行为 | RDK X5 行为 |
|------|------|------|
| **成功发送命令** | `wait_send_shumeipai()` 阻塞等待回复 | 收到命令 → 执行视觉任务 → `send_data()` 发回 |
| **视觉端超时未回复** | `wait_send_shumeipai()` 死等（无超时） | — |
| **视觉未识别到物体** | `RxPacket[8]==0x34` → 重发命令 | 超时 8s 后发 `"000000004"` |
| **视觉识别到物体** | 跳出循环，继续执行 | 发回坐标+颜色 |
| **颜色验证不匹配** | 不执行（当前注释） | `run_wukuaiyuanxin_xuanzequyu()` 循环等待直到匹配 |
| **颜色验证匹配** | 收到"98" → 跳出等待 | 发 `"987654321"` |
| **串口无数据** | 死等 | — |
| **程序异常** | — | 收到 `"r"` → 20s 后重启 |

---

## 六、全流程时空图

```
初始化 ─→ 等开关 ─→ IMU归零 ─→ 补光灯开
  │
  ├─①─ 扫码位移动(150, -530)
  │    task=[1,2,3,1,2,3] ← 硬编码（无视觉版）
  │    完整版：0x31 → RDK X5 扫码回传任务码
  │
  ├─②─ 圆台取料
  │    退910mm → 升臂250° → 抓1(红) → 存144°
  │    → 抓2(绿) → 存258° → 抓3(蓝)
  │    → 关灯复位
  │
  ├─③─ 穿越通道
  │    退 → 进400 → 转90° → 直走1700mm → 转180°
  │
  ├─④─ 色环放置(第一轮)
  │    角度校准 → 放红(193°) → 放绿(248°) → 放蓝(301°)
  │    → 复位193°
  │
  ├─⑤─ 色环取回
  │    取红 → 取绿 → 取蓝 → 平台归零
  │
  ├─⑥─ 返回
  │    退 → 走蓝线820 → 转90° → 走760
  │
  ├─⑦─ 色环放置(第二轮) → 复位
  │
  ├─⑧─ 返回圆台
  │    退 → 走蓝线870 → 转0° → 走390 → 云台278° → 前进靠墙
  │
  └─⑨─ jixie_reset() → while(1){} 结束
```
