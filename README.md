    

# 2026 年广东省工创大赛 · 智能物流搬运机器人 资料库

本仓库收录「智能物流搬运」赛项（MBH 项目）的完整参赛资料，涵盖 **结构设计、电控程序、视觉程序、任务命题文档、现场比赛经验** 五大板块。

> 项目主控：STM32F407（底层运动控制）＋ 地瓜派RDK X5（视觉识别），麦轮底盘 + 舵机机械臂 + CAN 总线电机。

---

## 一、仓库结构总览

```
gongchuangsai_BMH/
├── 结构件/                  # 机械结构原始设计文件（CATIA / SolidWorks）
├── 打印部件/                # 3D 打印件模型（STP / STEP / SLDPRT / 3mf）
├── 电控程序/                # STM32F407 嵌入式工程（初赛版 / 决赛版）
├── 视觉程序/                # RDK X5 视觉主程序
│   ├── gongchuang_vision.py         # 初赛
│   └── gongchuang_vision_final.py   # 决赛
├── 图片/                    # 现场照片、地图、流程、评分表等
├── 全流程说明.md            # 系统架构、通信协议、完整任务流程
├── 2026年广东省工创大赛-设计文档MBH.docx
├── 说明文档byph.docx
├── 打印件摆盘.3mf
└── Drawingmoban.CATDrawing
```

---

## 二、结构设计

### 2.1 结构件（原始设计文件）

位于 [`结构件/`](./结构件/)：

| 文件                        | 说明             |
| --------------------------- | ---------------- |
| `zhua.CATPart`            | 机械夹爪三维模型 |
| `四爪夹爪环形.SLDPRT`     | 四爪夹爪（环形） |
| `Drawingmoban.CATDrawing` | CATIA 工程图模板 |

### 2.2 打印部件（3D 打印模型）

位于 [`打印部件/`](./打印部件/)，共 100+ 个零件，按功能大致分为：

- **夹爪类**：`四爪夹爪 4mm`、`2mm勾爪`、`30mm圆勾爪`、`圆勾爪`、`齿轮20x1.25 左/右爪`、`手爪固定板`、`手爪舵机固定架`、`gudinggouzhua` 等
- **平台/置物类**：`置物盘2A垫高`、`置物盘3A`、`置物盘3AHIGH`、`配重平台`、`配重盒底板`、`平台垫块`、`平台垫块支撑`
- **固定/连接类**：`固定基准板3A 4mm`、`锁紧固定板3A 4mm`、`底板3A 4mm`、`底板连接件1/2/3`、`直线导轨转接板`、`升降台固定板/转接板` 等
- **传动类**：`圆柱齿轮20×2`、`齿条模数2x20 40齿`、`齿轮20x1.25 舵机法兰盘`、`锁紧固定板带圆弧齿`
- **摄像头/传感类**：`二维码摄像头固定板C`、`向下摄像头平台`、`向下摄像头平台ov5647 3A`、`摄像头平台`、`串口屏固定板`
- **电机类**：`35电机平台`、`42步进电机支架`、`平推电机固定板35电机`、`平推转线板3.1`
- **其他**：`红物块`（比赛物块模型）、`轮胎垫片`、`舵机固定小板` 等

> 打印摆盘见 [`打印件摆盘.3mf`](./打印件摆盘.3mf)。

---

## 三、电控程序

位于 [`电控程序/`](./电控程序/)，为 STM32F407（Keil MDK）工程，含 **初赛版** 与 **决赛版** 两个版本：

| 版本   | 路径                                          | 说明                     |
| ------ | --------------------------------------------- | ------------------------ |
| 初赛版 | [`电控程序/BMH-初赛/`](./电控程序/BMH-初赛/) | 完整工程，含全部功能模块 |
| 决赛版 | [`电控程序/BMH-决赛/`](./电控程序/BMH-决赛/) | 决赛迭代版本             |

### 核心代码模块

| 模块                 | 路径                                                                   | 功能                                 |
| -------------------- | ---------------------------------------------------------------------- | ------------------------------------ |
| `USER`             | `main.c` / `car.c` / `car.h`                                     | 主程序、整车运动控制（麦轮、走线）   |
| `USER`             | `cmd_parser.c/h`                                                     | 命令解析                             |
| `USER`             | `exti.c/h`                                                           | 外部按键中断                         |
| `Hardware`         | `PWM.c/h`、`servo.c/h`                                             | 舵机 PWM 控制                        |
| `Hardware`         | `pid.c/h`                                                            | PID 控制                             |
| `Hardware`         | `swith.c/h`                                                          | 限位开关                             |
| `can_ZDT`          | `Emm_V5`、`can`、`circle_pid`、`yyb_move`、`fifo`、`board` | CAN 总线电机控制、环形运动、搬运移动 |
| `imu`              | `imu.c`、`wit_c_sdk.c`、`timer.c`、`REG.h`                     | WHT101 惯性测量单元（USART2）        |
| `SYSTEM`           | `Serial`、`usart`、`delay`、`sys`                              | 串口、延时、系统基础                 |
| `OLED` / `LED`   | `OLED.c`、`time_OLED.c`、`LED.c`、`light.c`                    | 显示与指示灯                         |
| `lanya`            | `hmi.c`、`usart5.c`                                                | 蓝牙 / HMI 串口屏通信                |
| `ckp`              | `uart6_ckp.c/h`                                                      | 串口屏                               |
| `CORE` / `FWLIB` | CMSIS + STM32F4 标准外设库                                             | 内核与底层驱动                       |

### 调试工具

- [`CMSIS-DAP/`](./电控程序/BMH-初赛/CMSIS-DAP/)：仿真器固件（X893 版 / 官方原版）、电路原理图、WIN7 驱动、DIY 教程。

---

## 四、视觉程序

视觉端跑在 **地瓜派RDK X5** 上。源码已收录：

| 版本 | 文件                                                                            | 说明                                              |
| ---- | ------------------------------------------------------------------------------- | ------------------------------------------------- |
| 初赛 | [`视觉程序/gongchuang_vision.py`](./视觉程序/gongchuang_vision.py)             | 二维码、转盘物块、色环、码垛                      |
| 决赛 | [`视觉程序/gongchuang_vision_final.py`](./视觉程序/gongchuang_vision_final.py) | 初赛能力 + 固定物料区三工位 + 成品区 Code128 转盘 |

STM32 通过 UART 下发单字节任务码，RDK X5 完成识别后按同一帧格式回传坐标、颜色或任务码。颜色约定：`1` 红、`2` 绿、`3` 蓝。决赛成品区条码 `1/2/3` 是独立编号，不走颜色映射。

### 4.1 运行环境

| 项目     | 说明                                                   |
| -------- | ------------------------------------------------------ |
| 板卡     | 地瓜派RDK X5                                           |
| 系统     | Ubuntu / Linux（V4L2）                                 |
| 语言     | Python 3                                               |
| 依赖     | `opencv-python`、`numpy`、`pyserial`、`pyzbar` |
| 串口     | `/dev/ttyUSB0`，9600 8N1                             |
| 下摄像头 | `/dev/video0`，扫任务二维码                          |
| 上摄像头 | `/dev/video2`，物块、色环、码垛、物料区和成品区条码  |
| OpenCV   | 打开优化，8 线程                                       |

定标时上摄像头按 MJPEG、30 FPS、双缓冲协商分辨率。物块/色环粗定标用 `640×480`，色环精细定标切到 `1920×1080`。决赛里 `/dev/video2` 由 `ensure_video2_camera()` 统一复用，避免重复打开。

板端启动：

```bash
python3 gongchuang_vision.py          # 初赛
python3 gongchuang_vision_final.py    # 决赛
```

程序起来后先发 `987654321`，表示视觉端就绪，然后循环等 STM32 指令。

### 4.2 通信协议

收发都是：

```
0xFF + 9 字节 ASCII + 0xFE
```

不足 9 字节的数据后面补 `0x00`。STM32 发单字符任务码，视觉端回 9 字节结果。常见回传：

| 内容                       | 格式                           | 含义                          |
| -------------------------- | ------------------------------ | ----------------------------- |
| 任务码                     | `132+3120` 一类 7 位码再补齐 | 二维码识别结果                |
| 物块/色环坐标              | `XXXXYYYYC`                  | X、Y 各 4 位，C 为颜色        |
| 转盘 / 物料 / 条码夹取顺序 | `ABC456789`                  | 三个工位相对位置              |
| 就绪 / 确认                | `987654321`                  | 上电就绪，或颜色/条码确认稳定 |
| 超时 / 失败                | `000000004`                  | 本次识别失败                  |

更细的阶段约定见 [`全流程说明.md`](./全流程说明.md) 第三、四、五章。

### 4.3 STM32 任务码

| 指令          | 函数                                   | 作用                                                     |
| ------------- | -------------------------------------- | -------------------------------------------------------- |
| `1`         | `run_erweima()`                      | 下摄像头扫 7 位任务码，连续 3 帧一致后回传               |
| `2`         | `run_wukuaiyuanxin_1()`              | 第一圈转盘，定第一个物块圆心和颜色                       |
| `a`         | `run_wukuaiyuanxin_1()`              | 第二圈转盘，同样先定第一个物块                           |
| `3`         | `LOOP1()`                            | 第一圈转盘转起来后，算三个物块抓取顺序                   |
| `4`         | `LOOP2()`                            | 第二圈转盘，按任务码后三位再算一遍顺序                   |
| `5` / `6` | `run_wukuaiyuanxin_xuanzequyu()`     | 第一圈抓完第 1、2 个后，确认下一个物块到了               |
| `b` / `c` | `run_wukuaiyuanxin_xuanzequyu()`     | 第二圈同样确认后两个物块                                 |
| `7`         | `run_sehuanyuanxin2()`               | 色环第一次粗定标，`640×480`                           |
| `8`         | `run_sehuanyuanxin2_centered()`      | 色环精细定标：半分辨率粗检 + 全分辨率局部精修            |
| `9` / `0` | `run_maduoyuanxin2()`                | 码垛圆环定标                                             |
| `d`         | `yunxing_tiaoxingma_dingbiao()`      | **决赛** 成品区 Code128 首次定标，回 `XXXXYYYYC` |
| `e`         | `yunxing_tiaoxingma_zhuanpan()`      | **决赛** 等条码变化，判向后回三次夹取位置          |
| `f` / `g` | `yunxing_tiaoxingma_xuanzequyu()`    | **决赛** 确认条码 `2`、`3` 已到夹取位          |
| `h`         | `run_maduoyuanxin2()`                | **决赛** 固定物料区定标                            |
| `i`         | `yunxing_sangeyuanxin_shibie_q123()` | **决赛** 识别 Q1/Q2/Q3，按当前轮任务码回夹取顺序   |
| `r`         | `restart_program()`                  | 关串口，20 秒后重启视觉程序                              |
| `end`       | 退出主循环                             | 本轮任务结束                                             |

初赛里 `d` 只回 `000000000`，直线纠偏没启用。决赛把 `d/e/f/g` 改成成品区条码流程，`h/i` 是固定物料区。

### 4.4 识别思路

- **二维码**：`/dev/video0` 转灰度，`pyzbar` 解码，连续 3 帧相同才认。
- **转盘物块**：Hough 圆 + HSV 红绿蓝面积。红用两段色相。位置和颜色都稳定后才回传。
- **三个物块颜色**：先看左下 A1、右下 A2，A3 用排除法补出来，再对照任务码算抓取顺序。
- **色环粗定标**：全图找圆，限制在画面中部有效区，颜色和位置累计稳定后发送。
- **色环精定标**：先在中心合法区内半分辨率粗找，再切回 `1920×1080` 局部精修圆心。
- **码垛 / 物料区定标**：圆环更小，半径大约 `20~27`。
- **固定物料区（决赛 `h/i`）**：Q1/Q2/Q3 三个固定工位只看颜色。任务 `i` 第一轮对照二维码 `A1/A2/A3`，第二轮对照 `A4/A5/A6`，回 `ABC456789`。
- **成品区 Code128（决赛 `d/e/f/g`）**：`pyzbar` 只解 Code128。`d` 记下当前条码和圆心；`e` 等条码变化后，按左下/右下/上方三个位置判向，目标顺序固定为 `1→2→3`；`f/g` 再确认后两个条码到了。

上摄像头在 RDK X5 上通常只能占一路。决赛用 `ensure_video2_camera()` 共用 `/dev/video2`，避免重复打开失败。

### 4.5 依赖安装

```bash
sudo apt update
sudo apt install -y python3-opencv python3-numpy python3-serial libzbar0
pip3 install pyzbar
```

串口和摄像头权限不够时：

```bash
sudo usermod -aG dialout,video $USER
```

重新登录后再跑。

---

## 五、任务命题文档

| 文件                                                                                | 说明                                      |
| ----------------------------------------------------------------------------------- | ----------------------------------------- |
| [`2026年广东省工创大赛-设计文档MBH.docx`](./2026年广东省工创大赛-设计文档MBH.docx) | 赛项设计文档 / 命题相关材料               |
| [`说明文档byph.docx`](./说明文档byph.docx)                                         | 完整任务流程、通信协议、成功/失败处理汇总 |
| [`全流程说明.md`](./全流程说明.md)                                                 | 项目说明文档                              |

---

## 六、现场比赛经验

现场照片与资料位于 [`图片/`](./图片/)：

| 文件                                                           | 内容                               |
| -------------------------------------------------------------- | ---------------------------------- |
| `现场.jpg`                                                   | 比赛现场全景                       |
| `现场地图.jpg`                                               | 场地地图                           |
| `现场流程.jpg`                                               | 比赛流程                           |
| `评分表.jpg`                                                 | 评分标准                           |
| `acsll码表.png`                                              | ASCII 码表（调试参考）             |
| `现场葫芦五兄弟.jpg`、`罪恶二维码板.jpg`、`自断一臂.jpg` | 现场记录（含场地道具、二维码板等） |

比赛关键经验与踩坑点集中在 [`全流程说明.md`](./全流程说明.md)：

- **系统架构**：STM32 管 CAN、PID、麦轮和舵机；RDK X5 管 OpenCV / pyzbar 识别，两边走 UART
- **通信协议**：`0xFF + 9 字节 ASCII + 0xFE`，单字节命令触发视觉任务
- **完整任务流程**：扫码取任务 → 圆台取料 → 穿越 1700mm 通道 → 色环放置/取回 → 返回，共 9 个阶段
- **失败处理**：视觉未识别、颜色不匹配、超时等场景的应对策略
- **注意事项**：`jixie_reset()` 末尾 `while(1){}` 会导致第二轮不运行等已知问题

---

## 七、快速导航

| 想了解什么             | 去看                                                                            |
| ---------------------- | ------------------------------------------------------------------------------- |
| 系统架构与通信协议     | [`全流程说明.md`](./全流程说明.md) 第一、三章                                  |
| 完整任务流程（9 阶段） | [`全流程说明.md`](./全流程说明.md) 第四、六章                                  |
| 初赛视觉主程序         | [`视觉程序/gongchuang_vision.py`](./视觉程序/gongchuang_vision.py)             |
| 决赛视觉主程序         | [`视觉程序/gongchuang_vision_final.py`](./视觉程序/gongchuang_vision_final.py) |
| 底层运动控制代码       | [`电控程序/BMH-决赛/USER/`](./电控程序/BMH-决赛/USER/)                         |
| 机械结构三维模型       | [`结构件/`](./结构件/) 与 [`打印部件/`](./打印部件/)                          |
| 现场照片与评分标准     | [`图片/`](./图片/)                                                             |
